for (let contadora = 1; contadora <= 10; contadora++) {
    console.log(`Tabuada do ${contadora}`);

    for (let mult = 1; mult <= 10; mult++) {
        console.log(`${contadora} X ${mult} = ${contadora * mult}`);
    }
    console.log('')
}
